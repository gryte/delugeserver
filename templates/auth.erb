<% # loop through each auth user data bag item %>
<% auth_users = Chef::DataBag.load( 'auth' ) %>
<% auth_users.each do |user| %>
<% auth_user = Chef::DataBagItem.load( 'auth', user[0] ) %>
<%= auth_user['id'] %>:<%= auth_user['password'] %>:<%= auth_user['level'] %>
<% end %>
